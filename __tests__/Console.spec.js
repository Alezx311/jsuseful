describe('JsUseful.Console module tests', () => {
	it('Console.log Example', () => {
		console.log('Some log message')

		expect(console.log).toBeDefined()
	})

	it('Console.error Example', () => {
		console.error('Some error message')

		expect(console.log).toBeDefined()
	})

	it('Console.warn Example', () => {
		console.warn('Some warn message')

		expect(console.log).toBeDefined()
	})

	it('Console.info Example', () => {
		console.info('Some info message')

		expect(console.log).toBeDefined()
	})

	it('Console.debug Example', () => {
		console.debug('Some debug message')

		expect(console.log).toBeDefined()
	})

	it('Console.dir Example', () => {
		console.dir('Some dir message')

		expect(console.log).toBeDefined()
	})

	it('Console.count Example', () => {
		console.count('Some count message')

		expect(console.log).toBeDefined()
	})

	it('Console.group Example', () => {
		console.group('Some group message')

		expect(console.log).toBeDefined()
	})

	it('Console.table Example', () => {
		console.table('Some table message')

		expect(console.log).toBeDefined()
	})

	it('Console.profile Example', () => {
		console.profile('Some profile message')

		expect(console.log).toBeDefined()
	})

	it('Console.time Example', () => {
		console.time('Some time message')

		expect(console.log).toBeDefined()
	})

	it('Console.timeStamp Example', () => {
		console.timeStamp('Some timeStamp message')

		expect(console.log).toBeDefined()
	})

	it('Console.timeEnd Example', () => {
		console.timeEnd('Some timeEnd message')

		expect(console.log).toBeDefined()
	})

	it('Console.trace Example', () => {
		console.trace('Some trace message')

		expect(console.log).toBeDefined()
	})
})
