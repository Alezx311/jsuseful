const { Functions } = require('../src')

describe('Test Functions.Generator', () => {
  it('Should return Generator', () => {
    expect(Functions.Generator).toBeDefined()
  })
})
describe('Test Functions.Text', () => {
  it('Should return Text', () => {
    expect(Functions.Text).toBeDefined()
  })
})
describe('Test Functions.Sort', () => {
  it('Should return Sort', () => {
    expect(Functions.Sort).toBeDefined()
  })
})
describe('Test Functions.Obj', () => {
  it('Should return Obj', () => {
    expect(Functions.Obj).toBeDefined()
  })
})
describe('Test Functions.Func', () => {
  it('Should return Func', () => {
    expect(Functions.Func).toBeDefined()
  })
})
describe('Test Functions.Num', () => {
  it('Should return Num', () => {
    expect(Functions.Num).toBeDefined()
  })
})
describe('Test Functions.Other', () => {
  it('Should return Other', () => {
    expect(Functions.Other).toBeDefined()
  })
})
