// import { ReplaceDict, ReplaceFunc } from './Enums'
// import { TextJoiners } from './Types'

// export class RegExpressions {

//   public static IsRegExp(regExp: RegExp): boolean {
//     return regExp instanceof RegExp
//   }
//   public static JoinResults(regExpResults: string[], joinChar: string = JoinChar): string {
//     if(!regExpResults?.[0]?.length) {
//       return ``
//     }

//     return regExpResults?.[0]?.join('')
//   }
//   public static Flags(flags?: string) {
//     return `${flags?.match(/[gim]/g)}`
//   }
//   public static createRegExp(char?: string | string[]) {
//     if(!char) {
//       return new RegExp(/\w/gi)
//     }
//     return Array.isArray(char) ?
//   }

//   public static match(str: string, regExp?: RegExp): string {
//     if
//   }
// }
